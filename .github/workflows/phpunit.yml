name: PHPUnit

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
    - name: Execute tests (Unit and Feature tests)
      run: |
        docker compose up -d
        docker compose exec web bash -c "cd /srv/blackjack && php artisan test"
    - name: docker compose down
      run: docker compose down -v
